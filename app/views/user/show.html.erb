<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <title>Idea Handler</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="">
    <meta name="author" content="">
  </head>
  <body>
    <div class="navbar navbar-inverse navbar-fixed-top">
      <div class="navbar-inner">
        <div class="container-fluid">
          <button type="button" class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a class="brand" href="#">Idea Handler</a>
          <div class="nav-collapse collapse">
            <p class="navbar-text pull-right">
              Logged in as <%= @user.email %>
            </p>
          </div>
        </div>
      </div>
    </div>

    <div class="container-fluid">
      <div class="row-fluid">
        <div class="span3">
          <div class="well sidebar-nav">
            <ul class="nav nav-list">
              <li class="nav-header">Sidebar</li>
              <li ><%= link_to  "Search for new idea", searchidea_path%></li>
              <li><%= link_to "Create new idea", newidea_path %></li>
              <li><%= link_to "Sign out", destroy_user_session_path, :method => :delete %></li>
            </ul>
          </div><!--/.well -->
        </div><!--/span-->
        <div class="span9">
          <div class="hero-unit">
           <b><h3>Welcome!</h3> </b>
           <%= @user.email %>
          </div>
            <div class="span4">
              <h3> your created ideas </h3>
              <div style="overflow-x: hidden; overflow-y: auto; width: 51%; height: 220px;">
               <% @initiate.each do |initiat| %>
                 Name :<%= initiat.name %><br/>
                 Description :<%= initiat.description %><br/>
                 <%= link_to "Delete That idea", delete_path(:id => initiat.id) %>
                 <%= link_to "Update That idea", edit_path(:id => initiat.id) %><br/>
                 <br/>
               <% end %>
              </div>
            </div><!--/span-->
            <div class="span4">
              <h3> Ideas followed by you </h3>
               <div style="overflow-x: hidden; overflow-y: auto; width: 51%; height: 220px;">
                 <% @follow.each do |follow| %>
                    Name :<%= follow.name %><br/>
                    Description :<%= follow.description %><br/>
                    <br/>
                 <% end %>
               </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
